{
  "name": "wealthup-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install --prefix backend",
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2"
  }
}